server:
  port: 8083

spring:
  application:
    name: msComprador
  datasource:
    url: jdbc:mysql://localhost:3306/comprador_bd
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

springdoc:
  api-docs:
    enabled: true
    version: openapi_3_0
  swagger-ui:
    enabled: true      

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/     