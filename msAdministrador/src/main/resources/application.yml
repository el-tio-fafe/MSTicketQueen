server:
  port: 8080

spring:
  application:
    name: msAdministrador
  datasource:
    url: jdbc:mysql://localhost:3306/administrador_bd
    username: root
    password: ""
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

# --- CONFIGURACIÓN DE EUREKA CLIENT ---
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/




